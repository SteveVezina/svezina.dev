# svezina.dev

Technical field notes on agent infrastructure, identity, gateways, sandboxes, and distributed systems.

## Development

```bash
npm install
npm run dev
```

Posts live in `content/writing/*.mdx`.

## Example

`examples/secretless-agent` is the companion one-click-deploy example for the first essay.
