import Link from 'next/link'; import type {PostMeta} from '@/lib/posts'; export function PostList({posts}:{posts:PostMeta[]}){return <div className="post-list">{posts.map(p=><article className="post-row" key={p.slug}><div className="meta">{p.date} · {p.readingMinutes} min</div><div><h3><Link href={`/writing/${p.slug}`}>{p.title}</Link></h3><p>{p.description}</p></div></article>)}</div>}
