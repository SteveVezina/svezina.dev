import Link from 'next/link'; import type {PostMeta} from '@/lib/posts'; export function PostList({posts}:{posts:PostMeta[]}){return <div className="post-list">{posts.map(p=><article className="post-row" key={p.slug}><Link className="post-thumb" href={`/writing/${p.slug}`} aria-label={p.title}>{p.thumbnail?<img src={p.thumbnail} alt=""/>:<span>SV</span>}</Link><div className="post-copy"><div className="meta">{p.date} · {p.readingMinutes} min</div><h3><Link href={`/writing/${p.slug}`}>{p.title}</Link></h3><p>{p.description}</p></div></article>)}</div>}
