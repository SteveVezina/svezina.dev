import Link from 'next/link'; import {Container} from '@/components/container'; import {PostList} from '@/components/post-list'; import {getPublishedPosts} from '@/lib/posts'; export default async function Home(){const posts=await getPublishedPosts();return <Container><section className="page hero"><div className="eyebrow">Steve Vezina · field notes</div><h1 className="page-title">Infrastructure for software that can act.</h1><p className="page-deck">Notes on agents, identity, gateways, sandboxes, distributed systems, and the infrastructure behind AI.</p></section><section className="section"><div className="section-head"><h2>Latest writing</h2><Link className="meta" href="/writing">All writing →</Link></div><PostList posts={posts}/></section></Container>}
