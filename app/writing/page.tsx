import {Container} from '@/components/container'; import {PostList} from '@/components/post-list'; import {getPublishedPosts} from '@/lib/posts'; export const metadata={title:'Writing'}; export default async function Writing(){return <Container><section className="page"><div className="eyebrow">Writing</div><h1 className="page-title small-title">Field notes.</h1><p className="page-deck">Architecture, experiments, and opinions from building platforms for humans and agents.</p></section><section className="section"><PostList posts={await getPublishedPosts()}/></section></Container>}
